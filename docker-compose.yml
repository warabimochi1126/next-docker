version: '3'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile

    volumes: 
      - type: bind
        source: ./app
        target: /app

    working_dir: /app

    command: bash